<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_website_form_inherit_guest_checkout" model="ir.ui.view">
        <field name="name">website.form.inherit.guest.checkout</field>
        <field name="model">website</field>
        <field name="inherit_id" ref="website.view_website_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='language_ids']" position="after">
                <field name="enable_guest_checkout"/>
                <field name="enable_company_vat_visible" invisible="not enable_guest_checkout"/>
                <field name="enable_set_delivery_date" invisible="not enable_guest_checkout"/>
            </xpath>
        </field>
    </record>
</odoo>