<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Report Action -->
    <record id="action_report_picking_card" model="ir.actions.report">
        <field name="name">Sale For Picking Report</field>
        <field name="model">sale.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">alt_product_image_for_sale.report_pick_order_document</field>
        <field name="report_file">alt_product_image_for_sale.report_pick_order_document</field>
        <field name="print_report_name">'Sale For Picking - %s' % (object.name)</field>
        <field name="binding_model_id" eval="False"/>
        <field name="binding_type">report</field>
    </record>

</odoo>